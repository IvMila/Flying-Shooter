using System.Collections.Generic;
using UnityEngine;

public class LaserController : MonoBehaviour
{
    //[SerializeField] private List<LaserScriptableObject> _laserScriptableObjects;
    //public static int IndexLaser;
    //public PlayerController PlayerController;

    //private void Update()
    //{
    //    PlayerController = FindObjectOfType<PlayerController>();
    //}
    //public void CreateLaser()
    //{
    //    foreach (var index in _laserScriptableObjects)
    //    {
    //        if (IndexLaser == index.IndexLaser)
    //        {
    //            Debug.Log($"index: {index.IndexLaser}, index2 {IndexLaser}");
    //            GameObject newLaser = Instantiate(index.PrefabLaser);
    //            newLaser.transform.position = PlayerController.transform.position;
    //            Debug.Log(newLaser.transform.position);
    //        }
    //    }
    //}
}
